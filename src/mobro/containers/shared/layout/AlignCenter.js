import Container from "mobro/lib/component/container";
import AlignCenter from "mobro/components/shared/layout/AlignCenter";

export default Container.create("shared.layout.align-center", AlignCenter)
    .basic(false)
    .generate();