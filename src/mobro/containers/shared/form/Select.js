import Container from "mobro/lib/component/container";
import Select from "mobro/components/shared/form/Select";

export default Container.create("shared.form.select", Select)
    .basic(false)
    .generate();