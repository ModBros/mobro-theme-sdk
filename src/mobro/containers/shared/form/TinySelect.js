import Container from "mobro/lib/component/container";
import TinySelect from "mobro/components/shared/form/TinySelect";

export default Container.create("shared.form.tiny-select", TinySelect)
    .basic(false)
    .generate();