import Container from "mobro/lib/component/container";
import MobroIcon from "mobro/components/shared/MobroIcon";

export default Container.create("shared.mobro-icon", MobroIcon)
    .basic(false)
    .generate();