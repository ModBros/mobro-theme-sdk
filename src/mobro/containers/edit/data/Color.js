import Container from "mobro/lib/component/container";
import Color from "mobro/components/edit/data/Color";

export default Container.create("edit.data.color", Color)
    .basic(false)
    .generate();