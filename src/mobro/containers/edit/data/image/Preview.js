import Container from "mobro/lib/component/container";
import Preview from "mobro/components/edit/data/image/Preview";

export default Container.create("edit.data.image.preview", Preview)
    .basic(false)
    .generate();